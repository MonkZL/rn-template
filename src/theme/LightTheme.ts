import {IThemeTypes} from './index';

const LightTheme: IThemeTypes = {
  textDefaultColor: '#000000',
  textReverseColor: '#FFFFFF',
  textActiveColor: '#0D87FB',
  textInactiveColor: '#A1A4A9',
  bgDefaultColor: '#FFFFFF',
};

export default LightTheme;
